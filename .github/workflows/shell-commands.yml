name: Shell Commands Workflow

on: push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello World"
        
      - name: multi lines script
        run: |
          npm -v
          node -v